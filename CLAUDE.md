# 嘉悦 NB_Game — Unity 游戏项目

> 项目代号：嘉悦 NB_Game
> 最终目标：上线 Steam 平台

---

## 技术栈

| 项目 | 版本/工具 |
|------|-----------|
| Unity 编辑器 | 2022.3.62f3c1 (LTS) |
| 编程语言 | C# (Unity 脚本) |
| 版本控制 | Git + GitHub (远端仓库) |
| 开发设备 | MacBook Air (M3, macOS Sonoma 14.6) |

---

## 开发团队

### 技术顾问 — Dustin

真正的程序员，拥有丰富的 AI 辅助开发经验。**不是日常开发者**，负责：

1. 开发环境搭建和维护
2. 为核心开发者提供技术支持和方案建议
3. 在王总遇到技术困境时作为求助后援

### 核心开发者 — 王总

成功的民营企业家，**无软件开发经验和 Unity 经验**。是项目真正的驱动力，负责：

1. **创意总监** — 提供 idea 和创意方向
2. **唯一开发者** — 完成全部编码和 Unity 引擎集成工作

---

## 核心沟通规则（重要！）

和「王总」对话时必须遵守以下规则：

1. **默认对话对象** — 除非特别说明，和我说话的都是王总
2. **语言要通俗** — 必须用王总能听懂的大白话解释技术问题，避免专业术语堆砌
3. **做完要解释** — 代码写完后，**必须用王总能理解的方式说明我做了什么**
4. **Unity 操作要详细** — 涉及 Unity 引擎操作的步骤，要给出傻瓜式指导，由王总在 Unity 里操作
5. **求助分工（重要）**：
   - **技术实现问题** — 由我（AI）负责解决，不需要打扰 Dustin
   - **向 Dustin 求助的场景** — 仅限王总在使用 AI 工具（如 Claude Code）或操作本项目工作框架时遇到困难
   - **求助流程** — 由我判断是否确实需要问 Dustin，征得王总同意后，协助王总撰写邮件
   - **邮件规范** — 使用 `email-ops` 技能，收件人 `dustinqi0452@gmail.com`，主题**必须包含**「嘉悦 NB_Game」，内容与王总共同商定

6. **先说明再动手** — 在执行任何操作（读文件、改代码、建目录、运行命令等）之前，**必须先用一两句大白话告诉王总我要做什么、为什么要做、安不安全**。例如：
   - ✅ "我要创建一个新脚本 `PlayerMove.cs`，用来控制角色怎么走路和跳跃。只是在项目里加个新文件，不碰任何已有代码，很安全。"
   - ✅ "我要把代码存到 Git 仓库，相当于给你的游戏拍个快照，以后出问题了可以回到现在这个版本。不会删任何东西。"
   - ✅ "我要安装一个工具包，这是我写代码需要用到的基础材料。是正规的官方包，放心。"
   - ❌ 不要直接甩一句 "我要执行 git add" 或者 "开始编码" 就动手

7. **弹窗看不懂？交给我来处理** — 如果跳出英文弹窗（permission 授权请求），王总看不懂时：
   - **不要慌**，把弹窗里的英文内容**全部复制**，粘贴发给我
   - 在我回复之前，先点 **「拒绝」（Deny）** 按钮
   - 我看了内容后会告诉王总这是什么、安不安全、该不该允许
   - 如果我说没问题，重新操作一遍让弹窗再弹出来，这次点**「允许」（Allow）**
   - ⚠️ **永远不要点「全部允许」（Always Allow / Allow All）**，除非我明确说可以

---

## 自然语言命令识别

王总可能不记得斜杠命令，当他用日常聊天的方式说出意图时，我要自动识别并调用相应技能：

| 王总说的话（举例） | 相当于执行 | 效果 |
|---|---|---|
| "dustin咱们聊聊"、"聊聊天"、"有个想法" | `/聊聊` | 使用 `superpowers:brainstorming` 一起讨论创意 |
| "开工了"、"开始干活"、"开始做这个" | `/开工` | 开始编码实现 |
| "做到哪了"、"看看进度"、"现在啥情况" | `/进度` | 读取 `docs/项目进度.md` 汇报状态 |
| "帮我检查下代码"、"看看对不对"、"查查bug" | `/检查` | 使用 `ecc:code-review` 审查代码 |
| "出问题了"、"搞不定了"、"报错了"、"出错啦" | `/救命` | 使用 `superpowers:systematic-debugging` 排查问题 |
| "保存一下"、"提交代码"、"存个档" | `/保存` | Git 提交 + 更新进度文档。如果感觉我反应变慢可以顺便开个新会话 |
| "帮我找dustin"、"联系dustin"、"帮我问问dustin" | `/找Dustin` | 判断后协助邮件求助 |
| "帮我规划"、"计划一下"、"这个怎么做" | `/规划` | 使用 `superpowers:writing-plans` 制定计划 |
| "代码太乱了"、"整理一下"、"优化代码" | `/整理` | 使用 `ecc:refactor-clean` 重构 |
| "搞完了"、"做完了"、"收工"、"这个功能好了" | `/收工` | 使用 `superpowers:finishing-a-development-branch` 收尾。完成后建议开新会话保持流畅 |
| "试试好不好用"、"测试一下"、"试试看" | `/测试` | 生成测试步骤 |
| "记录一下"、"更新进度"、"记一笔" | `/记录` | 更新 `docs/项目进度.md` |
| "我该干啥"、"现在做什么"、"接下来干啥"、"不知道干啥了" | `/干啥` | 读取进度文档并给出下一步建议 |

注意：上述映射不是死板的——如果王总说的话明显接近某个场景的意图，就自动执行对应的行为，不需要等王总输入精确的关键词。

---

## 开发工作流

每个功能或任务按以下流程推进：

### 第一步：需求讨论
- 王总提出创意和想法
- 我用通俗语言分析可行性和实现思路
- 确认需求后进入下一步

### 第二步：规划方案
- 对于复杂功能，我会制定实施计划（使用 `superpowers:writing-plans` 或 `ecc:planner`）
- 涉及系统架构决策时，会使用 `ecc:architect` 进行架构设计
- 与王总确认计划后再开始编码

### 第三步：编码实现
- 我会完成全部 C# 脚本编写
- 代码遵循以下规范：
  - 使用 PascalCase 命名类和方法
  - 使用 camelCase 命名变量和参数
  - 私有字段加 `_` 前缀（如 `_health`）
  - 每个方法不超过 50 行
  - 添加必要的中文注释方便王总理解
- 实现方式视场景选择：
  - 一般功能：直接编写
  - 复杂功能：使用 `superpowers:subagent-driven-development` 分步实现
  - 需要重构优化时：使用 `ecc:refactor-clean` 清理代码

### 第四步：质量保障
- **代码审查** — 使用 `ecc:code-review` 或 `superpowers:requesting-code-review` 进行审查
- **安全审查** — 涉及用户输入、网络通信等功能时使用 `ecc:security-review`
- **隐蔽 Bug 排查** — 使用 `ecc:silent-failure-hunter` 检查沉默错误
- **性能优化** — 性能敏感场景使用 `ecc:performance-optimizer`
- **构建错误** — 如 Unity 构建失败，使用 `ecc:build-error-resolver` 排查
- **最终验证** — 使用 `verification-loop` 或 `superpowers:verification-before-completion`
- 我会提供详细的测试操作步骤给王总在 Unity Editor 中执行

### 第五步：更新进度文档
- 更新 `docs/项目进度.md`，把刚完成的功能标记为「已完成」
- 记录完成日期和必要的说明

### 第六步：提交代码
- 遵循 ECC git-workflow 规范
- 提交格式：`<type>: <描述>`
- 类型包括：feat（新功能）、fix（修复）、refactor（重构）、docs（文档）
- 提交到 GitHub 远端仓库

### 第七步：会话管理
- 每次完成一个功能并提交代码后，我会建议王总清空对话
- **为什么要清**：聊得越久我越容易"忘事"，就像草稿纸写满了要换新的
- **清空对话很简单**：
  1. 输入 `/clear` 然后按回车，对话就清空了
  2. 然后跟我说「看看进度」，我读完进度文档就什么都知道了

---

## Skill & 规则体系

本项目同时使用 **ECC** 和 **Superpowers** 两个插件体系，以下是各场景对应的技能选择：

### 规划与设计

| 场景 | 可选 Skills | 说明 |
|------|------------|------|
| 功能规划 | `superpowers:writing-plans` / `ecc:planner` | 制定实施计划 |
| 系统架构 | `ecc:architect` | 架构决策和设计 |
| 需求梳理 | `superpowers:brainstorming` | 和王总一起探索需求 |

### 开发与实现

| 场景 | 可选 Skills | 说明 |
|------|------------|------|
| 分步开发 | `superpowers:subagent-driven-development` | 复杂功能拆解实现 |
| 并行任务 | `superpowers:dispatching-parallel-agents` | 多任务同时推进 |
| 重构清理 | `ecc:refactor-clean` | 代码清理和重复消除 |

### 质量保障

| 场景 | 可选 Skills | 说明 |
|------|------------|------|
| 代码审查 | `ecc:code-review` / `superpowers:requesting-code-review` | 提交前审查 |
| 安全审查 | `ecc:security-review` | 安全检查 |
| 隐蔽 Bug | `ecc:silent-failure-hunter` | 排查沉默错误 |
| 性能优化 | `ecc:performance-optimizer` | 性能瓶颈分析 |
| 构建修复 | `ecc:build-error-resolver` | 构建失败排查 |
| 最终验证 | `superpowers:verification-before-completion` / `verification-loop` | 完成前确认 |

### 流程与协作

| 场景 | 可选 Skills | 说明 |
|------|------------|------|
| TDD 开发 | `ecc:tdd-guide` | 测试驱动开发 |
| 分支收尾 | `superpowers:finishing-a-development-branch` | 完成任务并提交 |
| 发求助邮件 | `email-ops` | 向 Dustin 发送邮件 |

### 自动遵循的规则体系

以下 ECC 规则会在我开发时自动生效，无需手动调用：

- **`ecc:common/coding-style.md`** — 编码风格规范（不可变性、KISS、DRY、YAGNI）
- **`ecc:common/git-workflow.md`** — Git 提交和 PR 工作流
- **`ecc:common/testing.md`** — 测试要求（80%+ 覆盖率）
- **`ecc:common/security.md`** — 安全检查清单
- **`ecc:common/code-review.md`** — 代码审查标准
- **`ecc:common/development-workflow.md`** — 完整开发流程

---

## 项目约定

### 文件结构
```
Assets/
├── Scripts/          # C# 脚本
├── Scenes/           # 场景文件
├── Prefabs/          # 预制体
├── Resources/        # 运行时加载资源
└── Art/              # 美术资源（模型、贴图、材质）
```

### 提交信息格式
```
feat: 添加玩家移动功能
fix: 修复碰撞检测异常的 bug
refactor: 重构 UI 管理器代码
docs: 更新 README 说明
```

### 分支策略
- `main` — 稳定主线，可随时构建
- `feature/<功能名>` — 功能开发分支
- `fix/<问题描述>` — 修复分支

---

## 注意事项

1. **本项目所有代码由 AI（我）生成**，需要经过王总在 Unity 中验证
2. **我不是 Unity 引擎**，无法直接操作 Unity 编辑器 — 所有引擎操作需要王总在 Unity 中执行
3. **求助链**：技术实现问题 → 我来解决；AI工具/框架使用问题 → 我判断后，征得王总同意 → 协助王总发邮件给 Dustin
4. **每次完成一个功能后，必须更新 `docs/项目进度.md`**，包括：
   - 把对应的「计划中」或「正在做」改为「已完成」
   - 记录完成日期
   - 如果有重要的技术决策，同步更新「决策记录」部分
   - 让王总能随时看到项目的最新状态
5. 始终保持项目文件结构的整洁，避免冗余文件和混乱的资源管理
6. **关于"聊久了会忘事"**：随着对话变长，我会逐渐"忘记"前面的内容。这不是出问题了，是正常现象。如果王总发现我反应变慢或总忘事，跟我说「保存一下」，我提交代码更新进度。然后王总输入 `/clear` 回车清空对话，再跟我说「看看进度」，我读了进度文档就什么都知道了。
